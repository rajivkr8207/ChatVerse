version: "3"

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: chatverse-backend
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=production
    command: npm start
